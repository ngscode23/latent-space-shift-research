# Latent RAPIDS Analyzer Runbook

Canonical documentation:

```text
scripts/analysis_tools/latent_gpu_rapids_analysis/README_latent_attractor_gpu_rapids_analysis.md
```

Script:

```text
scripts/analysis_tools/latent_gpu_rapids_analysis/latent_attractor_gpu_rapids_analysis.py
```

Use the canonical runbook above. This short file exists only as the central
`scripts/docs/` pointer so there is not a second stale copy of the same
instructions.
